apiVersion: v1
kind: Service
metadata:
  name: ti-apap
  namespace: default
  labels:
    app: ti-apap
spec:
  type: ClusterIP
  selector:
    app: ti-apap
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  sessionAffinity: None

